//
//  Canvas.swift
//  Drawing
//
//  Created by Martin Matějka on 24.08.16.
//  Copyright © 2016 Martin Matějka. All rights reserved.
//

import UIKit

class Canvas: UIView {
    
    
    
    // Only override drawRect: if you perform custom drawing.
    // An empty implementation adversely affects performance during animation.
   /* override func drawRect(rect: CGRect) {
        
        let context = UIGraphicsGetCurrentContext()
        CGContextSetLineWidth(context, 4.0)
        CGContextMoveToPoint(context, 10.0, 10.0)
        //CGContextSetFillColorWithColor(context, UIColor.blueColor().CGColor)
        CGContextSetStrokeColorWithColor(context, UIColor.blueColor().CGColor)
        let pointForRect: CGPoint = CGPoint(x: 50, y: 50)
        CGContextAddRect(context, CGRect(origin: pointForRect, size: CGSize(width: 40, height: 40)))
                //CGContextAddLineToPoint(context, 20.0, 180.0)
        CGContextStrokePath(context)
        
    }
    */
    
}
